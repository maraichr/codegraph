root = "."
tmp_dir = "tmp/worker"

[build]
  cmd = "go build -buildvcs=false -o ./tmp/worker/main ./cmd/worker"
  bin = "./tmp/worker/main"
  include_ext = ["go", "sql", "graphqls"]
  exclude_dir = ["tmp", "frontend", "node_modules", "deploy", "migrations"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
